{
  "name": "ch03-accessibility-tree",
  "version": "0.1.0",
  "description": "Ch03 accessibility tree と自然言語操作 (Claude Code × Playwright MCP 実践ガイド)",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "dump-snapshot": "playwright test tests/snapshot-introspect.spec.ts --reporter=list"
  },
  "devDependencies": {
    "@playwright/mcp": "0.0.73",
    "@playwright/test": "1.59.1",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
