{
  "name": "ch12-complete-project",
  "version": "0.1.0",
  "description": "Ch12 完成形プロジェクト (Claude Code × Playwright MCP 実践ガイド)",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test --project=Smoke",
    "test:default": "playwright test --project=Default",
    "test:ci": "CI=true playwright test"
  },
  "devDependencies": {
    "@playwright/mcp": "0.0.73",
    "@playwright/test": "1.59.1",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
